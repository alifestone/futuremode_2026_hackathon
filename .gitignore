# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Environment / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

CLAUDE.md
AGENT.md

# graphify — machine-local state (absolute paths, local mtimes, re-derivable cache)
graphify-out/.graphify_*
graphify-out/cache/
graphify-out/manifest.json
graphify-out/cost.json

# app — frozen snapshot package builds are reproducible via app/scripts/lock.py;
# the per-package reference-image copies are just self-containment conveniences
luna-comic/out/packages/*/ref/
luna-comic-2/out/packages/*/ref/
